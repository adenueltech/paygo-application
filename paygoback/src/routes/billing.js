// routes/billing.js
/*const express = require('express');
const router = express.Router();
const billingController = require('../controllers/billingController');

// Billing Routes
router.get('/current-usage', billingController.getCurrentUsage);
router.get('/invoices', billingController.getInvoices);
router.get('/invoices/:invoiceId', billingController.getInvoiceById);
router.get('/rate-cards', billingController.getRateCards);
router.post('/subscribe', billingController.subscribeToPlan);

module.exports = router;
*/